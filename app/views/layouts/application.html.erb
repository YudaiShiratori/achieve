<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>Sample</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    
   
    
  </head>

  <body>
    <div class="container">
        <header class="header">
          <nav class="navbar navbar-default">
            <div class="container-fluid">
              <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
                  <span class="sr-only">blog</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">miniBlog</a>
              </div>
              <div id="gnavi" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                  <li><%= link_to "TOP", tops_path %></li>
                  <li><%= link_to "一覧", blogs_path %></li>
                  <li><%= link_to "お問い合わせ", new_contact_path %></li>
                  <% if logged_in? %>
                      <li class="icon-bar"><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
                      <li class="icon-bar"><%= link_to 'お気に入り', user_path(current_user.id) %></li>
                  <% else %>
                      <li><%= link_to "新規登録", new_user_path %></li>
                      <li><%= link_to "ログイン", new_session_path %></li>
                  <% end %>
                </ul>
              </div>
            </div>
          </nav>
        </header>

      <%= yield %>
      
      <footer class="fixed-bottom">
        <p>白鳥雄大</p>
      </footer>
      
    </div>
  </body>
</html>